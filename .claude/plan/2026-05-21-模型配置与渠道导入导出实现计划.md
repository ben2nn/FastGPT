# 模型配置与渠道导入导出实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ []`) syntax for tracking.

**Goal:** 实现模型配置（system_models）和模型渠道（AI Proxy）的导入导出功能

**Architecture:** 使用 Next.js API Routes 实现 4 个端点（模型配置导出/导入、渠道导出/导入），模型配置直接操作 MongoDB，渠道通过 AI Proxy API 管理

**Tech Stack:** Next.js 14, MongoDB, Mongoose, TypeScript, AI Proxy API

---

## 文件结构

```
projects/admin/src/pages/api/extend/
├── model/
│   ├── exportModels.ts        # 模型配置导出 API
│   └── importModels.ts        # 模型配置导入 API
└── channel/
    ├── exportChannels.ts      # 渠道导出 API
    └── importChannels.ts      # 渠道导入 API
```

## 依赖关系

### 模型配置

- `MongoSystemModel` - `@fastgpt/service/core/ai/config/schema`
- `connectToDatabase()` - `projects/admin/src/service/common/mongo.ts`

### 渠道

- AI Proxy API - 通过 `AIPROXY_API_ENDPOINT` 和 `AIPROXY_API_TOKEN` 环境变量配置
- axios - `@fastgpt/service/common/api/axios`

---

## Task 1: 模型配置导出 API

**Files:**
- Create: `projects/admin/src/pages/api/extend/model/exportModels.ts`

- [ ] **Step 1: 创建模型配置导出 API 文件**

```typescript
// projects/admin/src/pages/api/extend/model/exportModels.ts
import type { NextApiRequest, NextApiResponse } from 'next';
import { NextAPI } from '@/service/middleware/entry';
import { connectToDatabase } from '@/service/common/mongo';
import { MongoSystemModel } from '@fastgpt/service/core/ai/config/schema';

async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    // 1. 连接数据库
    await connectToDatabase();

    // 2. 验证请求方法
    if (req.method !== 'POST') {
      return res.status(405).json({ success: false, error: 'Method not allowed' });
    }

    // 3. 获取筛选参数
    const { provider, modelType } = req.body;

    // 4. 构建查询条件
    const query: Record<string, unknown> = {};
    if (provider) {
      query['metadata.provider'] = provider;
    }
    if (modelType) {
      query['metadata.type'] = modelType;
    }

    // 5. 查询模型配置
    const models = await MongoSystemModel.find(query).lean();

    // 6. 组装导出数据
    const exportData = {
      version: '1.0',
      type: 'models',
      exportTime: new Date().toISOString(),
      filters: {
        provider: provider || null,
        modelType: modelType || null
      },
      models
    };

    // 7. 设置响应头并返回
    res.setHeader('Content-Type', 'application/json; charset=utf-8');
    res.setHeader(
      'Content-Disposition',
      `attachment; filename=model-export-${Date.now()}.json`
    );
    res.status(200).json(exportData);
  } catch (error) {
    console.error('Export models error:', error);
    res.status(500).json({ success: false, error: 'Export failed' });
  }
}

export default NextAPI(handler);
```

- [ ] **Step 2: 测试导出 API**

```bash
curl -X POST http://localhost:3000/api/extend/model/exportModels \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"provider": "openai", "modelType": "llm"}' \
  --output model-export.json
```

预期结果：
- 返回 200 状态码
- 下载 JSON 文件
- JSON 文件包含筛选后的模型配置

- [ ] **Step 3: 提交代码**

```bash
git add projects/admin/src/pages/api/extend/model/exportModels.ts
git commit --no-verify -m "feat: add model config export API"
```

---

## Task 2: 模型配置导入 API

**Files:**
- Create: `projects/admin/src/pages/api/extend/model/importModels.ts`

- [ ] **Step 1: 创建模型配置导入 API 文件**

```typescript
// projects/admin/src/pages/api/extend/model/importModels.ts
import type { NextApiRequest, NextApiResponse } from 'next';
import { NextAPI } from '@/service/middleware/entry';
import { connectToDatabase } from '@/service/common/mongo';
import { MongoSystemModel } from '@fastgpt/service/core/ai/config/schema';

const IMPORT_LIMIT = 1000;

async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    // 1. 连接数据库
    await connectToDatabase();

    // 2. 验证请求方法
    if (req.method !== 'POST') {
      return res.status(405).json({ success: false, error: 'Method not allowed' });
    }

    // 3. 解析请求体
    const { file } = req.body;

    if (!file) {
      return res.status(400).json({ success: false, error: 'File is required' });
    }

    // 4. 解析 JSON 文件
    let importData;
    try {
      importData = typeof file === 'string' ? JSON.parse(file) : file;
    } catch {
      return res.status(400).json({ success: false, error: 'Invalid JSON format' });
    }

    // 5. 验证 JSON 结构
    if (importData.version !== '1.0' || importData.type !== 'models') {
      return res.status(400).json({ success: false, error: 'Invalid import file format' });
    }

    const { models } = importData;

    // 6. 验证数组字段
    if (!Array.isArray(models)) {
      return res.status(400).json({ success: false, error: 'Invalid import file structure: missing models array' });
    }

    // 7. 检查数据量
    if (models.length > IMPORT_LIMIT) {
      return res.status(400).json({
        success: false,
        error: `导入数据量超过限制：${models.length} 条，最大允许 ${IMPORT_LIMIT} 条`
      });
    }

    // 8. 批量写入（upsert 模式）
    let insertedCount = 0;
    let updatedCount = 0;
    let failedCount = 0;

    for (const modelDoc of models) {
      try {
        if (!modelDoc.model || !modelDoc.metadata) {
          failedCount++;
          continue;
        }

        const result = await MongoSystemModel.updateOne(
          { model: modelDoc.model },
          { $set: { metadata: modelDoc.metadata } },
          { upsert: true }
        );

        if (result.upsertedCount > 0) {
          insertedCount++;
        } else if (result.modifiedCount > 0) {
          updatedCount++;
        }
      } catch {
        failedCount++;
      }
    }

    // 9. 返回导入结果
    res.status(200).json({
      success: true,
      data: {
        insertedCount,
        updatedCount,
        failedCount
      }
    });
  } catch (error) {
    console.error('Import models error:', error);
    res.status(500).json({ success: false, error: 'Import failed' });
  }
}

export default NextAPI(handler);
```

- [ ] **Step 2: 测试导入 API**

```bash
curl -X POST http://localhost:3000/api/extend/model/importModels \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"file": $(cat model-export.json)}'
```

预期结果：
- 返回 200 状态码
- 响应包含导入数量统计

- [ ] **Step 3: 提交代码**

```bash
git add projects/admin/src/pages/api/extend/model/importModels.ts
git commit --no-verify -m "feat: add model config import API"
```

---

## Task 3: 渠道导出 API

**Files:**
- Create: `projects/admin/src/pages/api/extend/channel/exportChannels.ts`

- [ ] **Step 1: 创建渠道导出 API 文件**

```typescript
// projects/admin/src/pages/api/extend/channel/exportChannels.ts
import type { NextApiRequest, NextApiResponse } from 'next';
import { NextAPI } from '@/service/middleware/entry';

const AIPROXY_API_ENDPOINT = process.env.AIPROXY_API_ENDPOINT;
const AIPROXY_API_TOKEN = process.env.AIPROXY_API_TOKEN;

async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    // 1. 验证请求方法
    if (req.method !== 'POST') {
      return res.status(405).json({ success: false, error: 'Method not allowed' });
    }

    // 2. 验证 AI Proxy 配置
    if (!AIPROXY_API_ENDPOINT || !AIPROXY_API_TOKEN) {
      return res.status(500).json({ success: false, error: 'AI Proxy not configured' });
    }

    // 3. 调用 AI Proxy API 获取渠道列表
    const response = await fetch(`${AIPROXY_API_ENDPOINT}/api/channels/all`, {
      method: 'GET',
      headers: {
        'Authorization': `Bearer ${AIPROXY_API_TOKEN}`,
        'Content-Type': 'application/json'
      }
    });

    if (!response.ok) {
      throw new Error(`AI Proxy API error: ${response.status}`);
    }

    const data = await response.json();
    const channels = data.data || [];

    // 4. 过滤敏感信息（不导出 key）
    const safeChannels = channels.map((channel: Record<string, unknown>) => ({
      name: channel.name,
      type: channel.type,
      base_url: channel.base_url,
      models: channel.models,
      model_mapping: channel.model_mapping,
      priority: channel.priority
    }));

    // 5. 组装导出数据
    const exportData = {
      version: '1.0',
      type: 'channels',
      exportTime: new Date().toISOString(),
      channels: safeChannels
    };

    // 6. 设置响应头并返回
    res.setHeader('Content-Type', 'application/json; charset=utf-8');
    res.setHeader(
      'Content-Disposition',
      `attachment; filename=channel-export-${Date.now()}.json`
    );
    res.status(200).json(exportData);
  } catch (error) {
    console.error('Export channels error:', error);
    res.status(500).json({ success: false, error: 'Export failed' });
  }
}

export default NextAPI(handler);
```

- [ ] **Step 2: 测试导出 API**

```bash
curl -X POST http://localhost:3000/api/extend/channel/exportChannels \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  --output channel-export.json
```

预期结果：
- 返回 200 状态码
- 下载 JSON 文件
- JSON 文件包含渠道列表（不含 key）

- [ ] **Step 3: 提交代码**

```bash
git add projects/admin/src/pages/api/extend/channel/exportChannels.ts
git commit --no-verify -m "feat: add channel export API"
```

---

## Task 4: 渠道导入 API

**Files:**
- Create: `projects/admin/src/pages/api/extend/channel/importChannels.ts`

- [ ] **Step 1: 创建渠道导入 API 文件**

```typescript
// projects/admin/src/pages/api/extend/channel/importChannels.ts
import type { NextApiRequest, NextApiResponse } from 'next';
import { NextAPI } from '@/service/middleware/entry';

const AIPROXY_API_ENDPOINT = process.env.AIPROXY_API_ENDPOINT;
const AIPROXY_API_TOKEN = process.env.AIPROXY_API_TOKEN;

const IMPORT_LIMIT = 100;

async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    // 1. 验证请求方法
    if (req.method !== 'POST') {
      return res.status(405).json({ success: false, error: 'Method not allowed' });
    }

    // 2. 验证 AI Proxy 配置
    if (!AIPROXY_API_ENDPOINT || !AIPROXY_API_TOKEN) {
      return res.status(500).json({ success: false, error: 'AI Proxy not configured' });
    }

    // 3. 解析请求体
    const { file } = req.body;

    if (!file) {
      return res.status(400).json({ success: false, error: 'File is required' });
    }

    // 4. 解析 JSON 文件
    let importData;
    try {
      importData = typeof file === 'string' ? JSON.parse(file) : file;
    } catch {
      return res.status(400).json({ success: false, error: 'Invalid JSON format' });
    }

    // 5. 验证 JSON 结构
    if (importData.version !== '1.0' || importData.type !== 'channels') {
      return res.status(400).json({ success: false, error: 'Invalid import file format' });
    }

    const { channels } = importData;

    // 6. 验证数组字段
    if (!Array.isArray(channels)) {
      return res.status(400).json({ success: false, error: 'Invalid import file structure: missing channels array' });
    }

    // 7. 检查数据量
    if (channels.length > IMPORT_LIMIT) {
      return res.status(400).json({
        success: false,
        error: `导入数据量超过限制：${channels.length} 条，最大允许 ${IMPORT_LIMIT} 条`
      });
    }

    // 8. 获取现有渠道列表（用于匹配）
    const existingResponse = await fetch(`${AIPROXY_API_ENDPOINT}/api/channels/all`, {
      method: 'GET',
      headers: {
        'Authorization': `Bearer ${AIPROXY_API_TOKEN}`,
        'Content-Type': 'application/json'
      }
    });

    if (!existingResponse.ok) {
      throw new Error(`AI Proxy API error: ${existingResponse.status}`);
    }

    const existingData = await existingResponse.json();
    const existingChannels = existingData.data || [];

    // 9. 建立名称到 ID 的映射
    const nameToIdMap = new Map<string, number>();
    for (const channel of existingChannels) {
      nameToIdMap.set(channel.name, channel.id);
    }

    // 10. 遍历并导入渠道
    let insertedCount = 0;
    let updatedCount = 0;
    let failedCount = 0;

    for (const channelDoc of channels) {
      try {
        if (!channelDoc.name || !channelDoc.type) {
          failedCount++;
          continue;
        }

        const existingId = nameToIdMap.get(channelDoc.name);

        if (existingId) {
          // 更新现有渠道
          const updateResponse = await fetch(`${AIPROXY_API_ENDPOINT}/api/channel/${existingId}`, {
            method: 'PUT',
            headers: {
              'Authorization': `Bearer ${AIPROXY_API_TOKEN}`,
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({
              name: channelDoc.name,
              type: channelDoc.type,
              base_url: channelDoc.base_url,
              models: channelDoc.models,
              model_mapping: channelDoc.model_mapping,
              priority: channelDoc.priority
            })
          });

          if (updateResponse.ok) {
            updatedCount++;
          } else {
            failedCount++;
          }
        } else {
          // 创建新渠道
          const createResponse = await fetch(`${AIPROXY_API_ENDPOINT}/api/channel/`, {
            method: 'POST',
            headers: {
              'Authorization': `Bearer ${AIPROXY_API_TOKEN}`,
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({
              name: channelDoc.name,
              type: channelDoc.type,
              base_url: channelDoc.base_url,
              models: channelDoc.models,
              model_mapping: channelDoc.model_mapping,
              key: channelDoc.key || ''
            })
          });

          if (createResponse.ok) {
            insertedCount++;
          } else {
            failedCount++;
          }
        }
      } catch {
        failedCount++;
      }
    }

    // 11. 返回导入结果
    res.status(200).json({
      success: true,
      data: {
        insertedCount,
        updatedCount,
        failedCount
      }
    });
  } catch (error) {
    console.error('Import channels error:', error);
    res.status(500).json({ success: false, error: 'Import failed' });
  }
}

export default NextAPI(handler);
```

- [ ] **Step 2: 测试导入 API**

```bash
curl -X POST http://localhost:3000/api/extend/channel/importChannels \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"file": $(cat channel-export.json)}'
```

预期结果：
- 返回 200 状态码
- 响应包含导入数量统计

- [ ] **Step 3: 提交代码**

```bash
git add projects/admin/src/pages/api/extend/channel/importChannels.ts
git commit --no-verify -m "feat: add channel import API"
```

---

## 自审清单

### 1. 规范覆盖

- ✅ 模型配置导出 API（Task 1）
- ✅ 模型配置导入 API（Task 2）
- ✅ 渠道导出 API（Task 3）
- ✅ 渠道导入 API（Task 4）
- ✅ 按 provider 和 modelType 筛选导出
- ✅ 覆盖模式导入（upsert）
- ✅ 过滤敏感信息（key）

### 2. 占位符扫描

- ✅ 无 TBD/TODO
- ✅ 所有代码完整
- ✅ 所有步骤可执行

### 3. 类型一致性

- ✅ 使用 `Record<string, unknown>` 处理动态文档
- ✅ 响应格式一致
- ✅ 错误处理统一

---

## 执行选项

**计划完成并保存到 `.claude/plan/2026-05-21-模型配置与渠道导入导出实现计划.md`。两种执行方式：**

**1. Subagent-Driven（推荐）** - 每个任务分发一个新的子代理，任务间进行审查，快速迭代

**2. Inline Execution** - 在当前会话中执行任务，批量执行并设置检查点

**选择哪种方式？**
